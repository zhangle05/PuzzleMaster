<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    tools:context=".fragments.LoadPuzzleFragment" >

    <TextView
        android:id="@+id/load_puzzle_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/load_puzzle"/>

    <ImageView
        android:id="@+id/load_puzzle_image"
        android:layout_width="320dp"
        android:layout_height="320dp"
        android:layout_below="@id/load_puzzle_label"
        android:layout_centerHorizontal="true"
        android:src="@drawable/no_image"/>

    <Button
        android:id="@+id/load_puzzle_button"
        android:layout_width="100dp"
        android:layout_height="50dp"
        android:layout_below="@id/load_puzzle_image"
        android:layout_centerHorizontal="true"
        android:text="@string/load"/>

</RelativeLayout>
